# HelloCSharp

A simple C# console calculator for COMP 019 (Activity 2).

It shows a menu and lets you add, subtract, multiply, divide, or get the
remainder of two numbers. It keeps asking until you choose Exit, and it checks
for invalid input and division by zero.

## How to run
```
dotnet run
```

`output.txt` shows a sample run.
